go to ~
create .vimrc file if not exist
add:
  set mouse=a  -> to add suport in vim file for scroll(press shift when copy/paste after this option was set)
  set background=dark -> to set background color

  #se tab indentation
  set background=dark
  set softtabstop=4
  set tabstop=4
  set shiftwidth=4
  set expandtab
