#see the enviromment that cron execute command
env -i /bin/bash --noprofile --norc in the shell lets you have the same environment as the one used by crontab, and this is super helpful to debug.

#to exit from this command
exit + enter

#if you need the log of cron use(file can be found in /home/user)
* * * * * whatever >> filename 2>&1
